[project]
name = "hackathon-sep-5"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "jinja2",
  "python-multipart",
  "python-dotenv",
  "langgraph",
  "langchain-anthropic",
  "langchain-openai",
  "pydantic",
  "requests",
]

[tool.vercel]
entrypoint = "main:app"
